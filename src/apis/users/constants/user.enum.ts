export enum UserRole {
  Admin = 'admin',
  Student = 'student',
}

export enum UserGender {
  Male = 'male',
  Female = 'female',
}

export enum UserLoginType {
  Email = 'email',
}

export enum UserStatus {
  Inactive = 'inactive',
  Active = 'active',
}
